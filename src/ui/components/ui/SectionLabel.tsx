import { styled } from '../../styles/stitches.config';

const SectionLabel = styled('h2', {
  margin: 0,
  fontSize: '$xs',
  fontWeight: 600,
  textTransform: 'uppercase',
  letterSpacing: '$wider',
  color: '$textFaint',
});

export { SectionLabel };
